import { SearchFilterPipe } from './search-filter.pipe';

describe('SearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe: SearchFilterPipe = new SearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

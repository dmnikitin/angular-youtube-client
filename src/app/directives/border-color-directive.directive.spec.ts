import { BorderColorDirectiveDirective } from './border-color-directive.directive';

describe('BorderColorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderColorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

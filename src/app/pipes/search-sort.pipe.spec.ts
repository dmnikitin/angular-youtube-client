import { SearchSortPipe } from './search-sort.pipe';

describe('SearchSortPipe', () => {
  it('create an instance', () => {
    const pipe: SearchSortPipe = new SearchSortPipe();
    expect(pipe).toBeTruthy();
  });
});
